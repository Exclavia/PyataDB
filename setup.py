from setuptools import setup, find_packages

setup(
    name='PynexDB',
    version='0.1.4',
    author='Exclavia',
    description='PynexDB is a lightweight, easy-to-use, and efficient database built entirely in Python',
    packages=find_packages(),
)
